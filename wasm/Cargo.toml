[package]
name = "rustsslcmd-wasm"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency WASM build of OpenSSL-compatible AES-256-CBC encrypt/decrypt"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
